# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Env / secrets — never commit real secrets (twelve-factor)
.env
backend/.env

# Local no-Docker verification infra (pgserver data dir, fakeredis TCP server
# log) — a one-off convenience for this sandbox, not part of the shipped app;
# docker-compose.yml is the real dev/deploy path (ARCHITECTURE.md D34).
.local-e2e/

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
