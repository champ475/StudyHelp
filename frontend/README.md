# StudyHelp frontend

Placeholder. React + TypeScript (Vite) structured step-input widget + streaming chat UI, built in Phase 4 (see `ARCHITECTURE.md` D12/D13 and the approved build plan). Kept as an empty sibling to `backend/` from commit one so Phase 4 doesn't require a repo restructure.
